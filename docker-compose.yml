services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # We name this so the 'web' container can find its files
    image: react_app_1-app:latest
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    networks:
      - web-network

  web:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    depends_on:
      - app
    ports:
      - "8080:80"
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
